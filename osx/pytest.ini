[pytest]
testpaths = tests
python_files = test_*.py
markers =
    darwin: requires macOS (darwin)
    mt02: plan 03 MT-02 Rich TUI subprocess/PTY tests
    mt09: plan 03 MT-09 legacy interactive PTY tests
    table_nav: Rich table Down + DEF-009 layout PTY (darwin; included in test-quick)
